package fr.usmb.m1isc.compilation.tp;

public enum NodeType {
    INTEGER,
    OPERATOR,
    PLUS,
    MULT,
    SUB,
    DIV,
    MOD,
    LET,
    VARIABLE,
    SEMI,
    IDENT,
    AND,
    OR,
    EGAL,
    GT,
    GTE,
    OUTPUT,
    INPUT,
    NIL,
    IF,
    THEN,
    ELSE,
    WHILE,
    NOT
    }
