package fr.usmb.m1isc.compilation.tp;

public enum NodeType {
    INTEGER, // OK
    OPERATOR, // OK
    PLUS, // OK
    MULT, // OK
    SUB, // OK
    DIV, // OK
    MOD, // OK
    LET, // OK
    VARIABLE, // OK
    SEMI, // OK
    IDENT, // OK
    AND,
    OR,
    EGAL, // OK
    GT, // OK
    GTE, // OK
    OUTPUT,
    INPUT,
    NIL,
    IF, // OK
    THEN, // OK
    ELSE, // OK
    WHILE,
    NOT
    }
